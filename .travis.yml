language: r
cache: packages
matrix:
  addons:
    apt:
      packages:
        - libgdal-dev
        - libproj-dev
        - libcurl4-openssl-dev
        - libgtk-3-dev
  r_binary_packages:
    - cairoDevice
    - RGtk2